worker_processes 2;

events { worker_connections 1024; }

http {

    upstream apollo_http {
        least_conn;
        server apollo_http_1 weight=10 max_fails=3 fail_timeout=30s;
    }
    upstream poseidon_http {
        least_conn;
        server poseidon_http_1 weight=10 max_fails=3 fail_timeout=30s;
    }
    server {
        listen 80;
        root /home/s/web/com/steven-luong;
        access_log /nginx/logs/access.log;
        error_log /nginx/logs/error.log;
        location /apollo {
            rewrite ^/apollo/(.*)$ /$1 break; 
            proxy_pass http://apollo_http;
        }
        location /poseidon {
            rewrite ^/poseidon/(.*)$ /$1 break; 
            proxy_pass http://poseidon_http;
        }

    }

}


